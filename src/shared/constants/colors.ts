/**
 * Default color export for backwards compatibility.
 * Components should use theme.colors via styled-components ThemeProvider.
 */
export { LuxeDark as Colors } from './themes';
export type { ThemeColors } from './themes';
